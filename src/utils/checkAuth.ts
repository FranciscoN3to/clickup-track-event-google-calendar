function checkAuth() {
    const token = localStorage.getItem('google-token')
    const refrashToken = localStorage.getItem('google-refresh-token')
    const calendarId = localStorage.getItem('google-calendar-id')
}